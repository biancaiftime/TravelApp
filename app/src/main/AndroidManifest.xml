<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proiecttandroid">

    <application
        android:name=".Controllers.ApplicationController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_bi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_bi_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.GalleryActivity"/>
        <activity android:name=".Activities.TripsActivity">
        </activity>
    </application>

</manifest>